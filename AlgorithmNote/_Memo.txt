# 큐(deque)에서 뽑아서 바로 넣기
queue.append(queue.popleft())

# 큐에서 특정 값의 인덱스 뽑기
queue.index("값")

# 풀이 방식 기록하기
# 풀이 : 직접풀이 / 답안 참고 / 부분 참고
# 이해도 : 높음 / 중간 / 낮음
# 시간 소요 : 00분

# Sys Input 사용하기
import sys
input = sys.stdin.readline

